# Sublime Text Configuration

Hit `CTRL+SHIFT+p` in sublime text and head to `Keybinds` text file and `Preferences` text file by simply typing in the the command palette.

Paste the `./keybindings.sublime-keymap` in the `Keybinds` file and similarly `./preferences.sublime-settings` for the `Preferences` file.
